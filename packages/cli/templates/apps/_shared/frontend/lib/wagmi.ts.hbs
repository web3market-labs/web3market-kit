import { http, createConfig } from 'wagmi'
import { mainnet, sepolia, arbitrum, base, polygon, optimism, hardhat } from 'wagmi/chains'

export const wagmiConfig = createConfig({
  chains: [hardhat, sepolia, mainnet, arbitrum, base, polygon, optimism],
  transports: {
    [hardhat.id]: http('http://127.0.0.1:8545'),
    [sepolia.id]: http(),
    [mainnet.id]: http(),
    [arbitrum.id]: http(),
    [base.id]: http(),
    [polygon.id]: http(),
    [optimism.id]: http(),
  },
  ssr: true,
})

declare module 'wagmi' {
  interface Register {
    config: typeof wagmiConfig
  }
}
