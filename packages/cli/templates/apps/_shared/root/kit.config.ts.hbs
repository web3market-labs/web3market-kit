import { defineConfig } from '@web3market/config'

export default defineConfig({
  contracts: {
    framework: 'foundry',
  },
  chains: {
    default: 'localhost',
  },
  components: [{{{componentsArray}}}],
  template: '{{templateId}}',
})
