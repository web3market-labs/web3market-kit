'use client'

import { useState } from 'react'
import { useAccount, useWriteContract, useWaitForTransactionReceipt } from 'wagmi'
import { parseEther } from 'viem'

export function ContributeForm() {
  const { address } = useAccount()
  const [amount, setAmount] = useState('')

  const { writeContract, data: hash, isPending } = useWriteContract()
  const { isLoading: isConfirming, isSuccess } = useWaitForTransactionReceipt({ hash })

  const handleContribute = () => {
    if (!amount || !address) return
    // TODO: Replace with your deployed presale contract address and generated ABI
    // writeContract({
    //   address: PRESALE_ADDRESS,
    //   abi: presaleAbi,
    //   functionName: 'contribute',
    //   value: parseEther(amount),
    // })
  }

  return (
    <div className="rounded-lg border bg-card p-6">
      <h3 className="text-lg font-semibold mb-4">Contribute ETH</h3>
      <div className="space-y-4">
        <div>
          <label className="text-sm font-medium mb-1.5 block">Amount (ETH)</label>
          <input
            type="text"
            value={amount}
            onChange={(e) => setAmount(e.target.value)}
            placeholder="0.0"
            className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
          />
          <p className="text-xs text-muted-foreground mt-1">
            Min: {{minContribution}} ETH — Max: {{maxContribution}} ETH
          </p>
        </div>
        <button
          onClick={handleContribute}
          disabled={isPending || isConfirming || !amount}
          className="inline-flex items-center justify-center rounded-md text-sm font-medium h-10 px-4 py-2 w-full bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50"
        >
          {isPending ? 'Confirming...' : isConfirming ? 'Contributing...' : 'Contribute'}
        </button>
        {isSuccess && (
          <p className="text-sm text-green-600">Contribution successful!</p>
        )}
      </div>
    </div>
  )
}
