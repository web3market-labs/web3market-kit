'use client'

import { PresaleProgress } from './PresaleProgress'
import { PresaleTimer } from './PresaleTimer'
import { ContributeForm } from './ContributeForm'
import { VestingPanel } from './VestingPanel'

export function PresaleDashboard() {
  return (
    <div className="space-y-6">
      <div className="grid gap-4 md:grid-cols-4">
        <div className="rounded-lg border bg-card p-4">
          <p className="text-sm text-muted-foreground">Hard Cap</p>
          <p className="text-2xl font-bold mt-1">{{hardCap}} ETH</p>
        </div>
        <div className="rounded-lg border bg-card p-4">
          <p className="text-sm text-muted-foreground">Soft Cap</p>
          <p className="text-2xl font-bold mt-1">{{softCap}} ETH</p>
        </div>
        <div className="rounded-lg border bg-card p-4">
          <p className="text-sm text-muted-foreground">Min Contribution</p>
          <p className="text-2xl font-bold mt-1">{{minContribution}} ETH</p>
        </div>
        <div className="rounded-lg border bg-card p-4">
          <p className="text-sm text-muted-foreground">Max Contribution</p>
          <p className="text-2xl font-bold mt-1">{{maxContribution}} ETH</p>
        </div>
      </div>

      <PresaleTimer durationDays={ {{presaleDurationDays}} } />
      <PresaleProgress hardCap={ {{hardCap}} } softCap={ {{softCap}} } />

      <div className="grid gap-6 md:grid-cols-2">
        <ContributeForm />
        <VestingPanel />
      </div>
    </div>
  )
}
